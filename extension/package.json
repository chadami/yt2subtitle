{
  "name": "@youtube-ai-subtitle/extension",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp manifest.json background.js contentScript.js popup.html popup.js options.html options.js dist/ && cp -R icons dist/"
  }
}
